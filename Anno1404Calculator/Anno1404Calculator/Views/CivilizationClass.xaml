<UserControl
    x:Class="Anno1404Calculator.Views.CivilizationClass"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Anno1404Calculator.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Background="LightBlue" ToolTipService.ToolTip="Inhabitants (live)">
        <Image Source="{x:Bind CivClassName, Converter={StaticResource CivilizationIconConverter}}" Stretch="Uniform" Width="50" Height="50" />
        <TextBlock Text="{x:Bind Amount, Mode=OneWay}" IsTextSelectionEnabled="True" VerticalAlignment="Center" />
    </StackPanel>
</UserControl>
